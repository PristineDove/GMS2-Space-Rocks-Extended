/// @description Initialize All Variables

//speed of rotation per frame in degrees
rotation_angle_speed = 5;

//acceleration of ship per frame in pixels
acceleration = .06;

//half the width of sprite for positioning sprite
half_sprite_width = sprite_width * .5;

//Checks if game is running
in_game = true;

//Sets ship friction
ship_friction = .01;

//Set ship max speed (2 pixels slower than bullet)
max_speed = 4;